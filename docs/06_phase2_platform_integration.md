# 06 · Phase 2 — Career-Path Platform Integration (DEFERRED)

> **Status: deferred.** This is a placeholder. Phase 2 is intentionally **out of scope**
> until the main project (Slices 1–2) is complete, per the project owner's decision.

## What Phase 2 will do
The company runs a platform that ingests a project's structure and surfaces it inside the
internal **career-path web app** (so a learner's progress through the milestones is
tracked and displayed there).

## What it will need (to be filled in when Phase 2 starts)
- The **example/reference repo** the platform expects (URL to be provided by the owner).
- A mapping from this repo's **milestones (M0–M10)** and **acceptance criteria** to the
  platform's progress model.
- Any required **manifest/metadata file(s)** the platform reads (format TBD from the
  example repo).
- CI hooks, if the platform expects status to be pushed on commit/PR.

## Why it's deferred
The main deliverable is the **learning project itself** (generator, medallion build,
studybook, dashboards). Locking the platform contract now would be premature; we revisit
once Slice 2 lands and the example repo is available.

_No action required from participants. This document will be replaced with the real
integration guide when Phase 2 begins._
